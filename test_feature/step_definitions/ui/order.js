const { When } = require('@cucumber/cucumber');
const { setTextBox, clickOnElement } = require('../../../utils/elements');
const { waitForLoader, waitForElement } = require(`../../../utils/wait`);

When(/^I see the homepage$/, async function () {
    
});
